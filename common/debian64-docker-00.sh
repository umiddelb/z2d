#!/bin/sh

# Yet to come, docker support for ARM in Debian 9 is broken at this time
#sudo apt-get -y install lxc aufs-tools cgroupfs-mount cgroup-bin apparmor 
#sudo apt-get -y -t stretch-backports install docker.io
#sudo usermod -aG docker debian
